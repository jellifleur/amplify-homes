type Target @model @auth(rules: [{allow: public}]) {
  id: ID!
  msgID: String
  groupID: String
  selector: String
  ko_KR: String
  en_US: String
  isModified: Boolean
}

type Home @model @auth(rules: [{allow: public}]) {
  id: ID!
  address: String
  image_url: String
  price: Float
}
 